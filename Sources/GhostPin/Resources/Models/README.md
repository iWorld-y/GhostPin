# GhostPin Models

Run `./script/fetch_models.sh` to download the default local speech model:

- `ggml-base-q5_1.bin`

Model files are intentionally ignored by git. The build script copies them into
`GhostPin.app/Contents/Resources/Models/` when present.
