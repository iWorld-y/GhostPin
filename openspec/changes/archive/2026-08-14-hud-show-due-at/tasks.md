## 1. HUD 时间行

- [x] 1.1 `DesktopNoteCardView` 时间行改为仅在 `dueAt` 存在时展示截止时间，格式保持 zh_CN `M月d日 HH:mm`
- [x] 1.2 移除创建时间与提醒时间的展示及对应 formatter
- [x] 1.3 过期任务的截止时间使用红色（与标题删除线同一套 `isOverdue()`）

## 2. 验收

- [x] 2.1 有截止日期的任务：卡片只显示截止时间，不显示创建时间与铃铛
- [x] 2.2 无截止日期的任务：卡片不显示任何时间
- [x] 2.3 已过期且未完成的任务：标题红删除线，截止时间同为红色
- [x] 2.4 `swift run TodoPinCoreChecks` 仍通过（本次不改 Core）
