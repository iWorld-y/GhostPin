## 1. 快捷键配置模型与持久化

- [x] 1.1 在 App 层新增可 Codable 的 `HotKeyShortcut` 值类型，保存 key code、修饰键和显示名称，并实现快捷键录制规则校验。
- [x] 1.2 在 `AppPreferences` 增加交互模式快捷键启用状态和可选快捷键配置，默认分别为关闭和空。
- [x] 1.3 实现快捷键配置的 UserDefaults 编解码，确保关闭开关时保留快捷键值且不改变任务数据。

## 2. 全局快捷键服务

- [x] 2.1 为 GhostPin App 恢复 Carbon 链接，并新增只管理一个快捷键的 App 层注册服务。
- [x] 2.2 实现普通非独占快捷键注册、注销和主线程回调，将触发动作交给 `AppState.toggleHUDMode()`。
- [x] 2.3 实现 macOS symbolic hot keys 预检和注册错误映射，区分已知系统冲突、注册失败和第三方冲突不可确认状态。
- [x] 2.4 在快捷键录制期间暂停当前快捷键注册；录制取消、校验失败或注册失败时恢复原有注册。

## 3. AppState 生命周期与事务编排

- [x] 3.1 在应用启动和退出生命周期中分别恢复和注销已启用的快捷键；默认关闭或配置为空时跳过注册。
- [x] 3.2 实现打开开关、录制新快捷键、关闭开关和重新启用的状态转换，确保待配置状态不落盘、不生效。
- [x] 3.3 实现“先检查/注册、后保存”的配置提交与失败回滚，替换已生效快捷键时保留旧配置可恢复。
- [x] 3.4 暴露快捷键运行时状态和错误信息，启动恢复失败时保留持久化配置并允许用户在设置页处理。

## 4. 设置页高级 Tab

- [x] 4.1 将现有设置内容整理为 HUD Tab，并新增“高级”Tab，保持现有 HUD 和登录启动设置行为不变。
- [x] 4.2 在高级 Tab 增加快捷键启用开关、当前快捷键展示、录制按钮和待配置提示；开关关闭时停止快捷键响应但保留配置。
- [x] 4.3 使用设置窗口范围内的 key-down 监听实现录制、Escape 取消、修饰键/功能键校验和候选值提交。
- [x] 4.4 展示注册成功、已知系统冲突、注册失败和第三方冲突无法完全检测等状态，不把成功注册显示为全局无冲突。
- [x] 4.5 将录制入口改为点击「当前快捷键」行触发（仅开关打开时可用），并新增清除按钮：删除快捷键配置、注销注册并将开关恢复为关闭。

## 5. 文档与规格同步

- [x] 5.1 更新 `README.md`、`README.en.md` 和 `AGENTS.md`，说明全局快捷键默认关闭、可选配置及冲突检测边界。
- [x] 5.2 在实现完成后通过 OpenSpec 归档或同步流程将 `ghost-hud` delta spec 合并到主规格，确保“零全局快捷键”描述被替换为新的默认关闭行为。

## 6. 验证

- [x] 6.1 执行 `swift build` 和 `swift run GhostPinCoreChecks`，确认现有核心逻辑与 CLI 不受影响。
- [ ] 6.2 手动验证首次启动默认状态、打开开关但未配置、合法录制并生效、非法录制、关闭后恢复和重启恢复。
- [ ] 6.3 手动验证已知系统快捷键提示、注册失败回滚、第三方软件冲突警告，以及快捷键触发时 HUD 模式和焦点行为与托盘开关一致。
- [x] 6.4 执行应用启动/打包验证，确认 Carbon 链接、登录启动和退出注销在 macOS 14+ 环境正常。
